# API base URL (Fastify) — used for REST calls.
VITE_API_URL=http://localhost:3001

# Socket.IO base URL. Optional — defaults to VITE_API_URL when unset.
# Set both to the same value unless WebSockets terminate on a separate host.
VITE_SOCKET_URL=http://localhost:3001
